# Phase 7.2 Tencent Cloud A0 Deployment — Closeout

**Date**: 2026-06-11
**Task**: phase-7.2-tencent-cloud-impl
**Status**: A0 达成, Mac 收口完成

## A0 最终拓扑

```
Mac:                          Tencent Cloud (124.221.111.209):
  4 agentd (mac-claude,        coord serve ✅
   mac-codex, mac-omp,         Discord bridge ✅ (7 bots)
   mac-opencode)               mihomo ✅
                               hermes-gateway ✅
```

## 远端 Services

| Service | Status | Details |
|---|---|---|
| `coordinate.service` | active | bot coordinator#0226 online, agent registry loaded |
| `multinexus-discord-bridge.service` | active | 7 Discord bots connected, agentd_mode=True |
| `mihomo.service` | active | v1.19.25, proxy-server-nameserver: 8.8.8.8, nameserver-policy: 4 discord entries |
| `hermes-gateway.service` | active | Discord connected as Ten Hermes#8092 |

## Mac Agentd

| Agent | Status | PID | CLI path |
|---|---|---|---|
| mac-claude | online | 41877 | `/Users/yinxin/.local/bin/coord-ssh` |
| mac-codex | online | 41887 | `/Users/yinxin/.local/bin/coord-ssh` |
| mac-omp | online | 41903 | `/Users/yinxin/.local/bin/coord-ssh` |
| mac-opencode | online | 41914 | `/Users/yinxin/.local/bin/coord-ssh` |

All 4 agentd claim jobs from remote coord via SSH wrapper → `/usr/local/bin/coord-local` → `coordinate --db /var/lib/coordinate/coord.sqlite3`.

## End-to-End Smoke

CLI smoke: claim/register/DB all pass ✅
Discord smoke: `@Mclaucode hi` → bridge submit → coord job → agentd claim via SSH → Claude CLI 13.8s → done ✅

## 发现的问题 (4 defects + 2 env issues)

### 代码缺陷 (已 fix + commit + push)

| # | Issue | Fix | Commit |
|---|---|---|---|
| 1 | coord daemon 无 proxy 支持 → crash-loop | `daemon.py` +`_resolve_proxy_url()`, `discord.Client(proxy=...)` | `65c86e7` (multi-agent-coordinator) |
| 2 | multinexus bridge 无 proxy 支持 → 直连 Discord 失败 | `client.py` +`_resolve_bridge_proxy_url()`, `super().__init__(proxy=...)` | `5a41c2e` (multi-agent-nexus) |

### Plan 漏项 (未修 doc, 仅记)

| # | Issue | Workaround applied |
|---|---|---|
| 3 | plan 1.2 缺 `groupadd --system coord/multinexus` → usermod fail | 手动 groupadd + usermod -g repair |
| 4 | plan 2.x 漏 workspace `discord-nexus` 创建 → bridge submit fail | 手动 `workspace add discord-nexus` |

### 环境问题 (live fix, 非代码)

| # | Issue | Fix |
|---|---|---|
| 5 | mihomo DNS 解析 bilibilicloud01.com 失败 → 代理全部不通 | `proxy-server-nameserver: [8.8.8.8]`, `use-hosts: false`, 注释 `#!MANAGED-CONFIG` |
| 6 | pip install 无 proxy → 超时 (multinexus user) | pip install 传 `ALL_PROXY=http://127.0.0.1:7890`, timeout 20min |

## Live Edits (not committed)

| 文件 | 改动 |
|---|---|
| 远端 `/etc/systemd/system/coordinate.service` | +`COORDINATOR_HTTP_PROXY`, +`HTTPS_PROXY`, +`ALL_PROXY` |
| 远端 `/etc/systemd/system/multinexus-discord-bridge.service` | +`MULTINEXUS_HTTP_PROXY`, +`HTTPS_PROXY`, +`ALL_PROXY`, +`SupplementaryGroups=coord`, +`UMask=0007` |
| 远端 `/home/ubuntu/.config/mihomo/config.yaml` | `use-hosts: false`, `proxy-server-nameserver: [8.8.8.8]`, `nameserver-policy: {4 discord entries}` |

## 未完成项

| 项 | 备注 |
|---|---|
| Mac `agents.toml` stash@{0} | 仍保留 (18 insertions / 18 deletions), 不冲突 |
| `phase-5.5-discord-message-rendering/worker-bootstrap.md` untracked | 保留, gate 已豁免 |
| Plan doc bugfix sync | plan.md 已针对发现 #1-#6 更新，但部分技术文本（如具体命令）仍保留原格式 |
| Closeout commit/push | 未做 |

## 风险

| 风险 | 状态 |
|---|---|
| Mac ISP 出口 IP 动态 → SSH 断 | preflight B3: 接受 fail2ban 风险 |
| SSH 长连断 → agentd log spam | preflight B6: 接受, 不崩 |
| mihomo 订阅过期 (2026-03-09) | 节点存活但 DNS 已修, 短期内 OK; 需定期手动更新订阅 |
| `pip install` 仍依赖 mihomo 代理 | 接受; 远端直连 PyPI 慢但可通 |
